/**
 *
 * @author Melanie
 */
public interface Movable {
    /**
     * Declares a single method and is implemented by entities that can move in the maze. 
     * @param direction 
     */
    void move(char direction);
}
